import { FoobarDirective } from './foobar.directive';

describe('FoobarDirective', () => {
  it('should create an instance', () => {
    const directive = new FoobarDirective();
    expect(directive).toBeTruthy();
  });
});
